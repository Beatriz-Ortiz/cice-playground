// Shows the current date in the console
import { getTime } from './date.js'

// Shows the number of seconds since the epoch time in the console
import { getTime as getEpochTime } from './seconds.js'

// Alerts your name
import { getName } from './name.js'

// Shows a confirm alert that tells the user that they are the millionth visitor
import getVisitor from './visitor.js'

// Asks the user to input a, b or c and loads a JavaScript file with
// that name and executes a default function which logs the name of the file
import { loadJavaScript } from './logsname.js'

getTime()
getEpochTime()
getName()
getVisitor()
loadJavaScript()
